:cachedir: /var/cache/r10k
:sources:
  hiera:
    prefix: false
    basedir: "<%= @hieradata_path %>"
    remote: "<%= @hiera_repo %>"
  puppet: 
    prefix: false
    basedir: <%= @r10k_env_basedir %>
    remote: "<%= @puppet_env_repo %>"

:purgedirs:
  - <%= @r10k_env_basedir %>
  - <%= @hieradata_path %>
